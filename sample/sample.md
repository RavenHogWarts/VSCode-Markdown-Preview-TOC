# Sample Workspace · Markdown Preview TOC

> M0 脚手架验证用。打开预览（`Ctrl+Shift+V`）后，DevTools Console 应出现 `[mdtoc] ...` 日志。

## 一、安装

### 1.1 通过 CLI 安装

pnpm 安装说明。

### 1.2 通过 VSIX 安装

离线安装说明。

## 二、使用

### 2.1 打开预览

`Ctrl+Shift+V` 打开预览。

### 2.2 切换 TOC 位置

修改 `markdownToc.sidebarPosition` 后自动刷新。

## 三、配置

### 3.1 宽度

`markdownToc.sidebarWidth`。

### 3.2 折叠

`markdownToc.defaultCollapsed`。

## 四、FAQ

### 4.1 TOC 不出现？

检查 DevTools Console。

### 4.2 滚动高亮闪烁？

收紧 `rootMargin`。

---

###### h6 示例标题（验证 maxDepth）

##### h5 示例标题

#### h4 示例标题
